language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-gulpular' ]; then cd .. && eval "mv $currentfolder generator-gulpular" && cd generator-gulpular; fi

